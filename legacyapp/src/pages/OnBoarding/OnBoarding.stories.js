import React from 'react';
import { storiesOf } from '@storybook/react-native'
import OnBoardingPage from './index'

storiesOf('PAGE | On Boarding', module)
  .add('default', () => <OnBoardingPage />)
